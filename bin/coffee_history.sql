CREATE TABLE COFFEE_HISTORY(
	SEQ NUMBER(4) PRIMARY KEY,
	COFFEE_NAME VARCHAR2(50) NOT NULL, 
	ID VARCHAR2(50) NOT NULL,
	ORDER_DATE VARCHAR2(30) NOT NULL,
	COFFEE_SIZE VARCHAR2(20) NOT NULL,
	COUNT NUMBER(3) NOT NULL,
	TOTALCOST NUMBER(6) NOT NULL
)

ALTER TABLE COFFEE_HISTORY ADD(DELNUM NUMBER(1));